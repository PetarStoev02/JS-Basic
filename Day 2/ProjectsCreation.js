function ProjectsCreation(name, num) {
    projectTime = Number(num * 3);
    console.log(
        `The architect ${name} will need ${projectTime} hours to complete ${num} project/s.`
    );
}
ProjectsCreation("Sanya", 9);
